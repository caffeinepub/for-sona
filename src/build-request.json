{
  "kind": "build_request",
  "title": "Add uploaded Kawaii sticker asset and fix Quiz sticker image path",
  "priority": "normal",
  "requirements": [
    {
      "id": "REQ-46",
      "text": "Add the user-uploaded sticker image file \"🖤 Kawaii Valentine's Stickers!-1.jpg\" to the frontend static assets under `frontend/public/assets/generated/` using a sanitized filename (lowercase, hyphen-separated, no spaces/special characters), and ensure it is directly loadable at `/assets/generated/<sanitized-filename>.jpg`.",
      "target": "frontend",
      "source": {
        "messageIds": [
          "msg-7"
        ],
        "quotes": [
          "yes do it",
          "The user has uploaded 1 image(s): 🖤 Kawaii Valentine's Stickers!-1.jpg."
        ]
      },
      "acceptanceCriteria": [
        "A new file exists at `frontend/public/assets/generated/kawaii-valentines-stickers-1.jpg` (or an equivalent sanitized filename that is lowercase, hyphen-separated, and contains no spaces/special characters).",
        "Requesting `/assets/generated/kawaii-valentines-stickers-1.jpg` in the deployed app returns the image (no 404)."
      ]
    },
    {
      "id": "REQ-47",
      "text": "Update `frontend/src/components/sections/QuizSection.tsx` so the corner sticker `<img>` `src` matches the actual sanitized asset path under `/assets/generated/` and the sticker reliably renders in both the quiz question card and the quiz results card.",
      "target": "frontend",
      "source": {
        "messageIds": [
          "msg-4",
          "msg-7"
        ],
        "quotes": [
          "i cant seem to see the image.",
          "yes do it"
        ]
      },
      "acceptanceCriteria": [
        "In `frontend/src/components/sections/QuizSection.tsx`, both sticker `<img>` elements currently using `src=\"/assets/generated/kawaii-valentines-stickers.jpg\"` are updated to the sanitized, real path (e.g. `/assets/generated/kawaii-valentines-stickers-1.jpg`).",
        "On the Quiz section, the sticker is visible on the question card view and remains visible on the results card view.",
        "No new user-facing fallback text is introduced for the sticker; if the image loads successfully, there is no broken-image icon shown."
      ]
    }
  ],
  "constraints": [],
  "nonGoals": [],
  "imageRequirements": {
    "required": [
      "Kawaii bear holding bouquet sticker (kawaii-valentines-stickers-1.dim_220x220.jpg)"
    ],
    "edits": []
  },
  "userProfileUpdate": {
    "goalsToAdd": [],
    "goalsToRemove": [],
    "preferencesToAdd": [],
    "preferencesToRemove": [],
    "miscToAdd": [],
    "miscToRemove": [],
    "fieldsToSet": {},
    "fieldsToDelete": []
  }
}